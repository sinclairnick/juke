import { ITokenizer } from "strtok3";
import { AbstractID3v2Parser } from "../id3v2/AbstractID3Parser";
import { INativeAudioMetadata, IOptions } from "../index";
import { Promise } from "es6-promise";
export declare class MpegParser extends AbstractID3v2Parser {
    private frameCount;
    private countSkipFrameData;
    private audioFrameHeader;
    private bitrates;
    private offset;
    private frame_size;
    private crc;
    private unsynced;
    private warnings;
    private calculateEofDuration;
    private samplesPerFrame;
    private metadata;
    private buf_frame_header;
    private tokenizer;
    /**
     * Number of bytes already parsed since beginning of stream / file
     */
    private mpegOffset;
    private readDuration;
    private syncPeek;
    /**
     * Called after ID3 headers have been parsed
     */
    _parse(metadata: INativeAudioMetadata, tokenizer: ITokenizer, options: IOptions): Promise<void>;
    /**
     * Called after file has been fully parsed, this allows, if present, to exclude the ID3v1.1 header length
     * @param metadata
     * @returns {INativeAudioMetadata}
     */
    protected finalize(metadata: INativeAudioMetadata): INativeAudioMetadata;
    private _peekBuffer;
    private _sync;
    private sync;
    private parseAudioFrameHeader;
    private parseCrc;
    private skipSideInformation;
    private readXtraInfoHeader;
    /**
     * Ref: http://gabriel.mp3-tech.org/mp3infotag.html
     * @returns {Promise<string>}
     */
    private readXingInfoHeader;
    private skipFrameData;
    private areAllSame;
}
