import { INativeAudioMetadata, IOptions } from "../index";
import { ITokenizer } from "strtok3";
import { AbstractID3v2Parser } from "../id3v2/AbstractID3Parser";
import { Promise } from "es6-promise";
export declare class FlacParser extends AbstractID3v2Parser {
    static getInstance(): FlacParser;
    private tokenizer;
    private options;
    private metadata;
    private tags;
    private padding;
    private warnings;
    _parse(metadata: INativeAudioMetadata, tokenizer: ITokenizer, options: IOptions): Promise<void>;
    private parseBlockHeader;
    private parseDataBlock;
    /**
     * Parse STREAMINFO
     */
    private parseBlockStreamInfo;
    /**
     * Parse VORBIS_COMMENT
     * Ref: https://www.xiph.org/vorbis/doc/Vorbis_I_spec.html#x1-640004.2.3
     */
    private parseComment;
    private parsePicture;
}
