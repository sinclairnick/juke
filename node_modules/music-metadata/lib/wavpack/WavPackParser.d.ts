import { INativeAudioMetadata, IOptions } from "../";
import { ITokenParser } from "../ParserFactory";
import { ITokenizer } from "strtok3";
import { Promise } from "es6-promise";
/**
 * WavPack Parser
 */
export declare class WavPackParser implements ITokenParser {
    private format;
    private tokenizer;
    private options;
    parse(tokenizer: ITokenizer, options: IOptions): Promise<INativeAudioMetadata>;
    parseWavPackBlocks(): Promise<INativeAudioMetadata>;
    private parseMetadataSubBlock;
}
