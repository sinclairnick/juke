import { ITokenParser } from "../ParserFactory";
import { INativeAudioMetadata, IOptions } from "../";
import { ITokenizer } from "strtok3";
import { Promise } from "es6-promise";
export declare class MP4Parser implements ITokenParser {
    private static read_BE_Signed_Integer;
    private static read_BE_Unsigned_Integer;
    private tokenizer;
    private options;
    private metaAtomsTotalLength;
    private format;
    private tags;
    private warnings;
    parse(tokenizer: ITokenizer, options: IOptions): Promise<INativeAudioMetadata>;
    parseAtom(parent: string[], size: number): Promise<void>;
    private parseAtomData;
    /**
     * Parse Metadata Atom (meta), ref: https://developer.apple.com/library/content/documentation/QuickTime/QTFF/Metadata/Metadata.html#//apple_ref/doc/uid/TP40000939-CH1-SW8
     * @param {string[]} parent Parent Atoms
     * @param {number} size Remaining meta atom size
     * @returns {Promise<void>}
     */
    private parseMetaAtom;
    private parseMetaAtomData;
    private parseAtom_ftyp;
    /**
     * Parse movie header (mvhd) atom
     * @param len
     */
    private parseAtom_mvhd;
    /**
     * Parse media header (mdhd) atom
     * @param len
     */
    private parseAtom_mdhd;
    private parse_mxhd;
    /**
     * Parse Meta-item-list-atom (item of 'ilst' atom)
     * @param len
     * Ref: https://developer.apple.com/library/content/documentation/QuickTime/QTFF/Metadata/Metadata.html#//apple_ref/doc/uid/TP40000939-CH1-SW8
     */
    private parseMetadataItemList;
    private parseMetadataItemData;
    private parseValueAtom;
}
