import { ITokenParser } from "../ParserFactory";
import * as strtok3 from "strtok3";
import { INativeAudioMetadata, IOptions } from "../index";
import { Promise } from "es6-promise";
export declare abstract class AbstractID3v2Parser implements ITokenParser {
    static startsWithID3v2Header(tokenizer: strtok3.ITokenizer): Promise<boolean>;
    private id3parser;
    parse(tokenizer: strtok3.ITokenizer, options: IOptions): Promise<INativeAudioMetadata>;
    /**
     * Called after ID3v2 headers are parsed
     */
    abstract _parse(metadata: INativeAudioMetadata, tokenizer: strtok3.ITokenizer, options: IOptions): Promise<void>;
    protected finalize(metadata: INativeAudioMetadata): INativeAudioMetadata;
    private parseID3v2;
    private tryReadId3v2Headers;
}
