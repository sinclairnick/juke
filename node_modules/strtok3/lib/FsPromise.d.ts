/// <reference types="node" />
import * as fs from "fs";
import { Promise } from "es6-promise";
export interface IReadResult {
    bytesRead: number;
    buffer: Buffer;
}
/**
 * Convert fs functions to promise based functions
 */
export declare class FsPromise {
    pathExists: typeof fs.existsSync;
    createReadStream: typeof fs.createReadStream;
    close(fd: number): Promise<void>;
    stat(path: fs.PathLike): Promise<fs.Stats>;
    open(path: fs.PathLike, mode?: string): Promise<number>;
    read(fd: number, buffer: Buffer, offset: number, length: number, position: number): Promise<IReadResult>;
    writeFile(path: fs.PathLike, data: Buffer | string): Promise<void>;
}
