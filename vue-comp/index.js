const album = require('./album');
const track = require('./track');
const selectedAlbum = require('./selectedAlbum');
const nowPlaying = require('./nowPlaying');
const nowPlayingArt = require('./nowPlayingArt');
const nextTrack = require('./nextTrack');
const prevTrack = require('./prevTrack');
const queueList = require('./queueList');
const addPlaylist = require('./addPlaylist');
const playlistInput = require('./playlistInput');
const playlist = require('./playlist');
const playlistNav = require('./playlistNav');


module.exports = {
    album,
    track,
    selectedAlbum,
    nowPlaying,
    nowPlayingArt,
    nextTrack,
    prevTrack,
    queueList,
    addPlaylist,
    playlistInput,
    playlist,
    playlistNav
}